# app_core

Reusable core architecture package for shared app logic.

## Includes

- `Result` and `Either`
- typed `Exceptions` and `Failures`
- `FailureMapper`
- `UseCase` + `NoParams`
- `ResultBloc`

## Usage

```dart
import 'package:app_core/app_core.dart';
```
