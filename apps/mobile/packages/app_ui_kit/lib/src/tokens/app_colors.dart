import 'package:flutter/material.dart';

/// Shared color tokens for the UI kit.
class AppColors {
  const AppColors._();

  /// Primary brand color used as the seed for the color scheme.
  static const Color primary = Color(0xFFE53935);
}
