name: app_env
description: "Reusable app_env package."
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  envied: ^1.3.4

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  envied_generator: ^1.3.4
  build_runner: ^2.13.1
