name: app_network
description: Reusable network layer with dio client and auth-ready interceptor.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  app_core:
    path: ../app_core
  app_storage:
    path: ../app_storage
  dio: ^5.9.2
  connectivity_plus: ^7.1.0
  internet_connection_checker: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
