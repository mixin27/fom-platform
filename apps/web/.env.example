API_BASE_URL="http://localhost:4000"
WEB_SESSION_SECRET="dev_fom_web_session_secret_change_me"
NEXT_PUBLIC_APP_BASE_URL="http://localhost:3000"

# Optional fallback used by metadataBase when NEXT_PUBLIC_APP_BASE_URL is missing
WEB_APP_BASE_URL="http://localhost:3000"

# Optional (used only for dev hints / copy)
PLATFORM_OWNER_EMAIL="owner@fom-platform.local"
PLATFORM_ADMIN_EMAIL="owner@fom-platform.local"
